# V6.6 Crop-based Smart Lab Importer

- Adds result-cell OCR for the Mahidol/Golden Jubilee Health Report table.
- OCR crops only the Result column, preventing reference values such as <42 from becoming patient results.
- Reads known rows independently with numeric-only OCR and plausibility validation.
- Keeps all imported rows unselected until the user confirms them against the source document.
- Confirmed imports are tagged `document-import-v6.6`.
- Service-worker cache bumped to `personal-health-v66-1`.
