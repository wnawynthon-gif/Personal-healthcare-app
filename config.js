/*
 * Public deployment configuration.
 *
 * Supabase Project URL and publishable/anon key are safe to use in a browser
 * when Row Level Security (RLS) is enabled. Never put a service_role key here.
 *
 * Fill these two values once before publishing to GitHub Pages. Every browser,
 * including iPhone Safari, will then connect automatically.
 */
window.PH_CONFIG = Object.freeze({
  supabaseUrl: "",
  supabaseAnonKey: ""
});
