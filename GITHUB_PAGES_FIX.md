# GitHub Pages deployment

Upload the contents of this folder to the repository root. `index.html`, `app.js`, `styles.css`, and `.nojekyll` must be beside `README.md` on the main branch.

Do not upload the ZIP itself and do not leave these files inside an extra `Personal-Healthcare-v9.2` folder.
